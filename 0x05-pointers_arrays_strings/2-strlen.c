#include <string.h>
#include "main.h"

/**
 * _strlen - gets the length of a string
 * @s: string to get the length
 *
 * Return: length of the string
 */
int _strlen(char *s)
{
	int l;

	l = strlen(s);

	return (l);
}
