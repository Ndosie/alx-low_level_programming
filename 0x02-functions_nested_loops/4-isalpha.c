#include "main.h"
/**
 * _isalpha - Checks if the character is alphabet
 * @c: The character to check
 *
 * Return: 1 if the character is alphabet and zero if the character is not
 */
int _isalpha(int c)
{
	return (c >= 'A' && c <= 'z');
}
