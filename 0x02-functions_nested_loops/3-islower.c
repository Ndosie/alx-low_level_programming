#include "main.h"

/**
 * _islower - Checks to see if a character is lower
 * @c: the character to check
 *
 * Return: 1 if is lower case or zero if it is not
 */
int _islower(int c)
{
	return (islower(c));
}
