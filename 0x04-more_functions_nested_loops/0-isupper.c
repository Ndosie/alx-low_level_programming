#include <ctype.h>
#include "main.h"

/**
 * _isupper - test the character if is upper case
 * @c: charater to test
 *
 * Return: zero or one
 */
int _isupper(int c)
{
	int i;

	i = isupper(c);

	return (i);
}
